![Coimisiún na Scrúdaithe Redbrick](https://i.imgur.com/IqjEiHx.png)


# Admin Root Exam: AGM 2026 - `username`

- Time allocated: `24 hours`
- Start: `18:00 27/03/2026`
- End: `18:00 28/03/2026`

> You must get 60% in each section to pass. Your overall mark doesn't matter.

> [ !NOTE ]
> This exam is open book. That doesn't mean you can blindly follow any resource you find online/chatGPT. 
> It's your job to use the various resources available to you to complete the given task
> Take the most up to date resource as accurate when it comes to docs / admin info

## Theory Section [75]

### 1. Linux 🐧 [15]
>I'd just like to interject for a moment. What you're refering to as Linux, is in fact, GNU/Linux, or as I've recently taken to calling it, GNU plus Linux.

1. What is POSIX?
2. A user cannot log in. What possible reasons do you check? Detail the full process you'd follow as a Redbrick admin. 
3. How would you find and terminate the process using the most memory?
4. You try to create a new file/save an existing file. You get an error `No space left on device`. You run `df -h` and see that the filesystem is only half full. What happened? Detail your full investigation and fix thought process.
5. Give a situation where you would use each of the following programs. Give examples of each:
    - `ps`
    - `tail`
    - `du`
    - `tmux`
    - `dig`
6. What is the best Linux distribution for use in servers?
    - Compare it to others that are used in industry

### 2. Redbrick [10]

1. What software does Redbrick use for the storage servers?
2. Name at least 5 active Redbrick servers
3. What handles the ingress in Redbrick? And what is the server called?
4. How are backups handled on `aperture`?
5. What is Nomad? How does Redbrick use Nomad? 
6. What is Consul? How does it integrate with Nomad?


### 3. Networking [10]
> It would be beneficial to your grading if you gave the usecase of each of the following in Redbrick (When applicable)

1. What is DHCP? What are its advantages over static addressing?
2. What is ARP?
3. What is DNS? What is an A record vs a CNAME record?
4. Design a network architecture for a small cluster system in Redbrick, consisting of the following components:
    - 3 compute nodes, 2 storage nodes, 2 login nodes, 2 network switches, 1 router
        - Explain the Vlan setup, NIC setup and how the routers should be used, Justify ur answer

### 4. Filesystems [15]

1. What RAID would you setup for the following number of disks: (Give your reasoning along with the amount of usable space)
    - 16   4TB drives
    - 18   1TB drives
    - 10   100GB drives and 5 200GB drives

2. Give different methods of file sharing on linux. Give the best one.
3. What is the difference between a filesystem and a partition?
4. Explain what these programs are used for:
	- `rsync`
    - `mount`
    - `head`
    - `chmod & chown`
    - `lsof`
5. What is the difference between ext4, ZFS, and Btrfs?

### 5. Git [12]

1. What is git? What are it's alternatives?
2. You have committed some changes to a git repository. You realise you left out some files from the commit, How would you proceed.
3. You accidentally committed a secret such as an API key. What should you do next?
4. You have your dotfiles in a github repo but want to take some files and features from another repo. How would you set up git to allow you to continuosly take new changes from the other repo into your repo? 
5. What does CI & CD stand for? Give one example of a tool used for each
6. You made local changes, but git pull fails because of conflicts. What does that mean?
7. You deleted a tracked file by mistake. How might you restore it?

### 6. Virtualisation [3]

1. What is the difference between Containers and VMs?
2. In the context of distributed HA systems, what is a virtual IP address? Provide examples of software that can be used to acheive this?
3. What is the role of a hypervisor? Give and example for and explain each type of hypervisor.


### 7. Misc [10]

1. A careless sysadmin executes the following command: `chmod 444 /bin/chmod` - what do you do to fix this?
2. You need to identify what package owns the file `/usr/bin/dig`. How would you do this on a Debian based system?
3. What kind of keys are in `~/.ssh/authorized_keys` and what it is this file used for?
4. Explain the following command `(date ; ps -ef |tail -n +2 | awk '{print $1}' | sort | uniq | wc -l ) >> Activity.log`
5. What is LDAP? How does it differ from AD? How does a RDN differ from a DN?
6. What is a Linux kernel module?
7. What is the difference between a symlink and a hardlink?
8. If you are running as root inside a docker container, can you run the `mknod` command that points to a block device on the host? Explain what kernel mechanism is at play here.

### :tada: Bonus Round! :tada:
> Not marked, but you should still attempt

1. As a new sysadmin, what cool new things would you like to improve/introduce in Redbrick? 
2. How many pints of Guinness is enough pints of Guinness?
3. What will happen on 19 January 2038?
4. Have ya ever had a pint of guinness?
5. How to fix mail?
---

## Practical Section [125]

> [!NOTE]
> Please include a brief summary of the steps you took to complete each question. Provide photo's when necessary.
> You MUST follow admin instruction for the duration of the exam. Failure to do so may lead to your your disqualification.

### 1. Security [15]

1. Secure your vm to your best ability

> There are many ways to accomplish this, the best approach is a well-rounded holistic one

### 2. LDAP [25]

1. Install an LDAP server on your VM
2. Set up an LDAP server
	- Use `o=brickie` for the organisation.
	- Create organisation units `groups` and `accounts`
	- Create an LDAP user called `resign` under the organisational unit `accounts`. Use the following credentials for them.

```
user: resign 
password: <Place the password of your VM here>
home: /home/resign
shell: /bin/bash


```

3. Allow `resign` to login over SSH through LDAP
	- This means LDAP user `resign` should be able to log into your VM via `ssh resign@$your-vm` with their password. They shouldn't need a local account to do this.
    - Their home directory should be created upon login if it does not exist.

4. Add resign to a Brickies group
    - Create a `brickies` group in the LDAP
    - Add `resign` to this group

5. Add extra fields for resign 
    - Add a gecos attribute for `resign` with his real name
    - the below fields should have data validation:
        - Add an `expiryDate` attribute for `resign` `(unix timestamp)`
        - Add a `year` attribute for `resign` `(yyyy)`
        - Add a `mail` and `altmail` attribute for `resign` `(localpart@hostname.local)`

### 3. File Share [10]

1. Set up your VM to provide storage over the network
2. Mount this file share to your local machine and transfer some files
	- Send proof of this (screenshot etc.)

### 4. Database [15]

1. Setup a standalone database for your applications to connect to - *you will be judged by the database you choose*
2. Create a user called `mirage`
3. Leave a special message for the admins and grant `mirage` access to it
4. Backup this database to a file. Paste the path to the file here:
```
// write your commands here
```

### 5. Docker [25]

1. Install docker on your vm
2. Set up [HedgeDoc](https://hedgedoc.org/) in a docker container and connect it to a sensible database - *or don't?*
	- any and all HedgeDoc data should be persistent
3. Grab the logs from the HedgeDoc container and paste them here.
```
// paste the logs here
```
4. Set up a reverse proxy of your choice in a docker container and connect it to HedgeDoc
    - Make sure the CSS for HedgeDoc is working
5. Set up HedgeDoc to use LDAP for auth
	- User `resign` should be able to login with their username and password

### 6. Nomad [15]

1. Install Nomad on your vm
2. Set up a single-node nomad deployment on your vm
3. Deploy an nginx web server using nomad serving static content
4. Proxy this web server through the reverse proxy you set up earlier *(any valid way of achieving this is okay)*
5. Write a script to display all nomad allocations and save their logs to a file. *(Do **NOT** use the web GUI)*

### 7. Scripts [20]

1. Write a script that takes in a job name and grabs Nomad logs from the most recent allocation of that job and displays only the IP addresses and MAC addresses found in these logs.
```
// write your script here
```

2. Write a cron job that triggers a backup job every 3 hours. The backup job should backup ALL the DBs running dumping them into into an encrypted archive on the file share
```
// write your script here
```

3. Write a script that updates user's shell in LDAP to `/usr/local/shells/expired` if their `expiryDate` field is less than the current date/time. `expiryDate` is of the format: `yyyymmddHHmmssZ`
```
// write your script here
```

4. A user hit their 5G quota on redbrick, and are complaining that they dont have enough space. Write a script that will purge all the cache and unnecessary files. Make sure none of the user's important data is deleted by mistake.
```
// write your script here
```

5. The admins can never seem to keep their docs up to date. Write a script that will parse the Nomad jobs in [Nomad repo](https://github.com/redbrick/nomad), and will alert the admins if there is no corresponding documentation in the [docs repo](https://github.com/redbrick/docs)
```
// write your script here
```

6. Write a fun script that will amuse us
```
// write your script here
```

